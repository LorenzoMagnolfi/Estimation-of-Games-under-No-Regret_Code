# Revision Track Notes

- External repo inspected: `https://github.com/Bo-Feng-1024/estimation_of_games_under_no_regret`
- Main branch is best treated as a fuller replication snapshot, not as a cleaned baseline code repo.
- Its `Code/master.do` and `Code/macros.do` match the broken path logic from the 2025 replication package.
- The repo includes extra generated outputs, vendored MATLAB dependencies, and documentation that may be useful as archive material.
- A branch named `Refactor-II_MAIN_simul.m` exists with commit `50560dc104080de7338b66f00a28a391530dfb56`.
- The branch commit message describes low-sigma "spiky distribution" experiments and documentation work, so it belongs in the revision track rather than the baseline track.
- The fetched branch did not expose an immediately superior baseline driver configuration; any port from that repo should be selective and change-by-change.
